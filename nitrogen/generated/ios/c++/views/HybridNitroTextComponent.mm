///
/// HybridNitroTextComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroTextComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroTextSpecSwift.hpp"
#import "NitroText-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitrotext;
using namespace margelo::nitro::nitrotext::views;

/**
 * Represents the React Native View holder for the Nitro "NitroText" HybridView.
 */
@interface HybridNitroTextComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroTextComponent {
  std::shared_ptr<HybridNitroTextSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroTextComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroTextComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridNitroTextShadowNode::defaultSharedProps();
    std::shared_ptr<HybridNitroTextSpec> hybridView = NitroText::NitroTextAutolinking::createNitroText();
    _hybridView = std::dynamic_pointer_cast<HybridNitroTextSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroText::HybridNitroTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroText::HybridNitroTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridNitroTextProps>(props);
  const auto* oldViewProps = static_cast<const HybridNitroTextProps*>(oldProps.get());
  NitroText::HybridNitroTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // fragments: optional
    if (oldViewProps == nullptr
          ? newViewProps.fragments.isProvided()
          : !newViewProps.fragments.hasSameValue(oldViewProps->fragments)) {
      swiftPart.setFragments(newViewProps.fragments.get());
    }
    // renderer: optional
    if (oldViewProps == nullptr
          ? newViewProps.renderer.isProvided()
          : !newViewProps.renderer.hasSameValue(oldViewProps->renderer)) {
      swiftPart.setRenderer(newViewProps.renderer.get());
    }
    // selectable: optional
    if (oldViewProps == nullptr
          ? newViewProps.selectable.isProvided()
          : !newViewProps.selectable.hasSameValue(oldViewProps->selectable)) {
      swiftPart.setSelectable(newViewProps.selectable.get());
    }
    // allowFontScaling: optional
    if (oldViewProps == nullptr
          ? newViewProps.allowFontScaling.isProvided()
          : !newViewProps.allowFontScaling.hasSameValue(oldViewProps->allowFontScaling)) {
      swiftPart.setAllowFontScaling(newViewProps.allowFontScaling.get());
    }
    // ellipsizeMode: optional
    if (oldViewProps == nullptr
          ? newViewProps.ellipsizeMode.isProvided()
          : !newViewProps.ellipsizeMode.hasSameValue(oldViewProps->ellipsizeMode)) {
      swiftPart.setEllipsizeMode(newViewProps.ellipsizeMode.get());
    }
    // numberOfLines: optional
    if (oldViewProps == nullptr
          ? newViewProps.numberOfLines.isProvided()
          : !newViewProps.numberOfLines.hasSameValue(oldViewProps->numberOfLines)) {
      swiftPart.setNumberOfLines(newViewProps.numberOfLines.get());
    }
    // lineBreakStrategyIOS: optional
    if (oldViewProps == nullptr
          ? newViewProps.lineBreakStrategyIOS.isProvided()
          : !newViewProps.lineBreakStrategyIOS.hasSameValue(oldViewProps->lineBreakStrategyIOS)) {
      swiftPart.setLineBreakStrategyIOS(newViewProps.lineBreakStrategyIOS.get());
    }
    // dynamicTypeRamp: optional
    if (oldViewProps == nullptr
          ? newViewProps.dynamicTypeRamp.isProvided()
          : !newViewProps.dynamicTypeRamp.hasSameValue(oldViewProps->dynamicTypeRamp)) {
      swiftPart.setDynamicTypeRamp(newViewProps.dynamicTypeRamp.get());
    }
    // maxFontSizeMultiplier: optional
    if (oldViewProps == nullptr
          ? newViewProps.maxFontSizeMultiplier.isProvided()
          : !newViewProps.maxFontSizeMultiplier.hasSameValue(oldViewProps->maxFontSizeMultiplier)) {
      swiftPart.setMaxFontSizeMultiplier(newViewProps.maxFontSizeMultiplier.get());
    }
    // adjustsFontSizeToFit: optional
    if (oldViewProps == nullptr
          ? newViewProps.adjustsFontSizeToFit.isProvided()
          : !newViewProps.adjustsFontSizeToFit.hasSameValue(oldViewProps->adjustsFontSizeToFit)) {
      swiftPart.setAdjustsFontSizeToFit(newViewProps.adjustsFontSizeToFit.get());
    }
    // minimumFontScale: optional
    if (oldViewProps == nullptr
          ? newViewProps.minimumFontScale.isProvided()
          : !newViewProps.minimumFontScale.hasSameValue(oldViewProps->minimumFontScale)) {
      swiftPart.setMinimumFontScale(newViewProps.minimumFontScale.get());
    }
    // menus: optional
    if (oldViewProps == nullptr
          ? newViewProps.menus.isProvided()
          : !newViewProps.menus.hasSameValue(oldViewProps->menus)) {
      swiftPart.setMenus(newViewProps.menus.get());
    }
    // onTextLayout: optional
    if (oldViewProps == nullptr
          ? newViewProps.onTextLayout.isProvided()
          : !newViewProps.onTextLayout.hasSameValue(oldViewProps->onTextLayout)) {
      swiftPart.setOnTextLayout(newViewProps.onTextLayout.get());
    }
    // onPress: optional
    if (oldViewProps == nullptr
          ? newViewProps.onPress.isProvided()
          : !newViewProps.onPress.hasSameValue(oldViewProps->onPress)) {
      swiftPart.setOnPress(newViewProps.onPress.get());
    }
    // onPressIn: optional
    if (oldViewProps == nullptr
          ? newViewProps.onPressIn.isProvided()
          : !newViewProps.onPressIn.hasSameValue(oldViewProps->onPressIn)) {
      swiftPart.setOnPressIn(newViewProps.onPressIn.get());
    }
    // onPressOut: optional
    if (oldViewProps == nullptr
          ? newViewProps.onPressOut.isProvided()
          : !newViewProps.onPressOut.hasSameValue(oldViewProps->onPressOut)) {
      swiftPart.setOnPressOut(newViewProps.onPressOut.get());
    }
    // text: optional
    if (oldViewProps == nullptr
          ? newViewProps.text.isProvided()
          : !newViewProps.text.hasSameValue(oldViewProps->text)) {
      swiftPart.setText(newViewProps.text.get());
    }
    // selectionColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.selectionColor.isProvided()
          : !newViewProps.selectionColor.hasSameValue(oldViewProps->selectionColor)) {
      swiftPart.setSelectionColor(newViewProps.selectionColor.get());
    }
    // fontSize: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontSize.isProvided()
          : !newViewProps.fontSize.hasSameValue(oldViewProps->fontSize)) {
      swiftPart.setFontSize(newViewProps.fontSize.get());
    }
    // fontWeight: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontWeight.isProvided()
          : !newViewProps.fontWeight.hasSameValue(oldViewProps->fontWeight)) {
      swiftPart.setFontWeight(newViewProps.fontWeight.get());
    }
    // fontColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontColor.isProvided()
          : !newViewProps.fontColor.hasSameValue(oldViewProps->fontColor)) {
      swiftPart.setFontColor(newViewProps.fontColor.get());
    }
    // fragmentBackgroundColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.fragmentBackgroundColor.isProvided()
          : !newViewProps.fragmentBackgroundColor.hasSameValue(oldViewProps->fragmentBackgroundColor)) {
      swiftPart.setFragmentBackgroundColor(newViewProps.fragmentBackgroundColor.get());
    }
    // fontStyle: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontStyle.isProvided()
          : !newViewProps.fontStyle.hasSameValue(oldViewProps->fontStyle)) {
      swiftPart.setFontStyle(newViewProps.fontStyle.get());
    }
    // fontFamily: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontFamily.isProvided()
          : !newViewProps.fontFamily.hasSameValue(oldViewProps->fontFamily)) {
      swiftPart.setFontFamily(newViewProps.fontFamily.get());
    }
    // lineHeight: optional
    if (oldViewProps == nullptr
          ? newViewProps.lineHeight.isProvided()
          : !newViewProps.lineHeight.hasSameValue(oldViewProps->lineHeight)) {
      swiftPart.setLineHeight(newViewProps.lineHeight.get());
    }
    // letterSpacing: optional
    if (oldViewProps == nullptr
          ? newViewProps.letterSpacing.isProvided()
          : !newViewProps.letterSpacing.hasSameValue(oldViewProps->letterSpacing)) {
      swiftPart.setLetterSpacing(newViewProps.letterSpacing.get());
    }
    // textAlign: optional
    if (oldViewProps == nullptr
          ? newViewProps.textAlign.isProvided()
          : !newViewProps.textAlign.hasSameValue(oldViewProps->textAlign)) {
      swiftPart.setTextAlign(newViewProps.textAlign.get());
    }
    // textTransform: optional
    if (oldViewProps == nullptr
          ? newViewProps.textTransform.isProvided()
          : !newViewProps.textTransform.hasSameValue(oldViewProps->textTransform)) {
      swiftPart.setTextTransform(newViewProps.textTransform.get());
    }
    // textDecorationLine: optional
    if (oldViewProps == nullptr
          ? newViewProps.textDecorationLine.isProvided()
          : !newViewProps.textDecorationLine.hasSameValue(oldViewProps->textDecorationLine)) {
      swiftPart.setTextDecorationLine(newViewProps.textDecorationLine.get());
    }
    // textDecorationColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.textDecorationColor.isProvided()
          : !newViewProps.textDecorationColor.hasSameValue(oldViewProps->textDecorationColor)) {
      swiftPart.setTextDecorationColor(newViewProps.textDecorationColor.get());
    }
    // textDecorationStyle: optional
    if (oldViewProps == nullptr
          ? newViewProps.textDecorationStyle.isProvided()
          : !newViewProps.textDecorationStyle.hasSameValue(oldViewProps->textDecorationStyle)) {
      swiftPart.setTextDecorationStyle(newViewProps.textDecorationStyle.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroText::NitroTextAutolinking::isNitroTextRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroText::HybridNitroTextSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
